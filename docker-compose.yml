version: '3.8'

services:
  # MySQL Database - DISABLED (Using XAMPP locally)
  # Uncomment below if you want to use Docker MySQL instead
  # mysql:
  #   image: mysql:8.0
  #   container_name: beehive-mysql
  #   restart: unless-stopped
  #   environment:
  #     MYSQL_ROOT_PASSWORD: ${MYSQL_ROOT_PASSWORD:-rootpassword}
  #     MYSQL_USER: ${MYSQL_USER:-beehive}
  #     MYSQL_PASSWORD: ${MYSQL_PASSWORD:-password}
  #     MYSQL_DATABASE: ${MYSQL_DATABASE:-beehive}
  #   ports:
  #     - "3306:3306"
  #   volumes:
  #     - mysql_data:/var/lib/mysql
  #     - ./scripts/init.sql:/docker-entrypoint-initdb.d/init.sql:ro
  #   command: --default-authentication-plugin=mysql_native_password --character-set-server=utf8mb4 --collation-server=utf8mb4_unicode_ci
  #   healthcheck:
  #     test: ["CMD", "mysqladmin", "ping", "-h", "localhost", "-u", "root", "-p${MYSQL_ROOT_PASSWORD:-rootpassword}"]
  #     interval: 10s
  #     timeout: 5s
  #     retries: 5

  # Redis Cache - DISABLED (Using local Redis/Memurai)
  # Uncomment below if you want to use Docker Redis instead
  # redis:
  #   image: redis:7-alpine
  #   container_name: beehive-redis
  #   restart: unless-stopped
  #   command: redis-server --appendonly yes
  #   ports:
  #     - "6379:6379"
  #   volumes:
  #     - redis_data:/data
  #   healthcheck:
  #     test: ["CMD", "redis-cli", "ping"]
  #     interval: 10s
  #     timeout: 5s
  #     retries: 5

  # API Server (Development) - DISABLED (Run with 'pnpm dev' instead)
  # Uncomment below if you want to run API in Docker
  # api:
  #   build:
  #     context: .
  #     dockerfile: ./apps/api/Dockerfile
  #   container_name: beehive-api
  #   restart: unless-stopped
  #   ports:
  #     - "4000:4000"
  #   environment:
  #     NODE_ENV: ${NODE_ENV:-development}
  #     DATABASE_URL: mysql://${MYSQL_USER:-beehive}:${MYSQL_PASSWORD:-password}@mysql:3306/${MYSQL_DATABASE:-beehive}
  #     REDIS_URL: redis://redis:6379
  #     JWT_SECRET: ${JWT_SECRET:-your-secret-key}
  #     API_PORT: 4000
  #   depends_on:
  #     mysql:
  #       condition: service_healthy
  #     redis:
  #       condition: service_healthy
  #   volumes:
  #     - ./apps/api:/app/apps/api
  #     - ./packages:/app/packages
  #     - /app/node_modules
  #     - /app/apps/api/node_modules

  # Web Frontend (Development) - DISABLED (Run with 'pnpm dev' instead)
  # Uncomment below if you want to run Web in Docker
  # web:
  #   build:
  #     context: .
  #     dockerfile: ./apps/web/Dockerfile
  #   container_name: beehive-web
  #   restart: unless-stopped
  #   ports:
  #     - "3000:3000"
  #   environment:
  #     NODE_ENV: ${NODE_ENV:-development}
  #     NEXT_PUBLIC_API_URL: ${NEXT_PUBLIC_API_URL:-http://localhost:4000}
  #     NEXT_PUBLIC_WALLET_CONNECT_ID: ${NEXT_PUBLIC_WALLET_CONNECT_ID:-}
  #   depends_on:
  #     - api
  #   volumes:
  #     - ./apps/web:/app/apps/web
  #     - ./packages:/app/packages
  #     - /app/node_modules
  #     - /app/apps/web/node_modules
  #     - /app/apps/web/.next

# Volumes - DISABLED (Using local services)
# Uncomment below if using Docker MySQL/Redis
# volumes:
#   mysql_data:
#     driver: local
#   redis_data:
#     driver: local
